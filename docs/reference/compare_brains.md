# compare_brains

compare_brains

## Usage

``` r
compare_brains(a, b, mask, ci = 0.95, legend_title = NULL)
```

## Arguments

- a:

  first image to compare

- b:

  second image to compare

- mask:

  mask image

- ci:

  confidence interval for bland-altman plots (default: 0.95)
