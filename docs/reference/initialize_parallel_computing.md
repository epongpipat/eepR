# initialize_parallel_computing

initialize parallel computing for different types of parallel computing

## Usage

``` r
initialize_parallel_computing(save = 1)
```

## Arguments

- save:

  number of cores to save/exclude from parallel computing (default: 1)
