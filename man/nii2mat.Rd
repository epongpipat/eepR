% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nii2mat.R
\name{nii2mat}
\alias{nii2mat}
\title{nii2mat
Convert a NIfTI File into a 2D Time-by-Voxel Matrix}
\usage{
nii2mat(in_path, in_mask = NULL, out_path = NULL, overwrite = FALSE)
}
\arguments{
\item{in_path}{Input path to 3D or 4D NIfTI file.}

\item{in_mask}{Input path to 3D binary mask NIfTI file. Optional. If NULL, all voxels are included.}

\item{out_path}{Optional input path to save the 2D matrix output (.rds format recommended).}

\item{overwrite}{Logical. If TRUE, overwrites an existing file at out_path. Default is FALSE.}
}
\value{
A 2D numeric matrix with dimensions \code{[T x V]}.
}
\description{
Reads a 3D or 4D NIfTI volume, applies a spatial 3D binary mask if available, and flattens
the in-mask data into a 2D matrix. The last dimension (Time or Diffusion direction)
is structured as rows, and the active spatial voxels are structured as columns.
Handles single-volume (3D) inputs by returning a 1-row matrix.
}
\examples{
# mat <- nii2mat("sub-01_task-rest_bold.nii.gz", "sub-01_brain_mask.nii.gz")
}
\seealso{
Other conversions between NIfTI and 2D-matrix:
\code{\link[=mat2nii]{mat2nii()}}
}
\concept{conversions between NIfTI and 2D-matrix}
\concept{neuroimaging}
